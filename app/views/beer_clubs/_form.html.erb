<%= form_with(model: beer_club) do |form| %>
  <% if beer_club.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
        <% beer_club.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="mb-3">
      <label for="Name" class="form-label"><%= form.label :name, style: "display: block" %>  <%= form.text_field :name %></label>
      <input type="text" class="form-control">
    </div>
    <div class="mb-3">
      <label for="founded" class="form-label"><%= form.label :founded, style: "display: block" %>   <%= form.number_field :founded %></label>
      <input type="text" class="form-control">
    </div>
    <div class="mb-3">
      <label for="founded" class="form-label">   <%= form.text_field :city %> <%= form.label :city, style: "display: block" %>
      </label>
      <input type="text" class="form-control">
    </div>
    <button type="submit" class="btn btn-primary"> <%= form.submit %></button>
  </form>
<%end %>