
<h2> Create new Beer </h2>

<%= form_for(@beer) do |f| %>
  <%= f.select :brewery_id, options_from_collection_for_select( @breweies, :id, :name) %>
  name: <%= f.text_field :name %>
  styles: <%= f.select :style, options_for_select(@styles) %>

  <%= f.submit %>
<% end %>
